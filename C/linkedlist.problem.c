#include<stdio.h>
struct node{
    int data;
    struct node* next;
}
struct node* head,*newnode,*temp;
while(choice) head=0,int{
    newnode=(struct node*) malloc (size of struct node);
    printf("Enter the data: ");
    scanf("%d",newnode->next);
    newnode->next=0;
    if(head=0){
        head=temp=newnode;
    }
    else{
        temp->next=newnode;
        temp=newnode;
    }
    print("Do you want to continue(0,1)");
    scanf("%d", &choice);

}
int main(){

    int data;
    print()
}