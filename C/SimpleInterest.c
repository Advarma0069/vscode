#include<stdio.h>
int main(){
    float p,r,t,si;  // creating of four boxes(4 dabba)
    p=100; //Principal values
    r=10;  // Rate of interest
    t=2;   // Time period
    si=(p*r*t)/100;
    printf("print the simple interest is :%f",si);
    return 0;
}